{
  "name": "svtrip-web",
  "private": true,
  "version": "0.1.0",
  "description": "SVTrip — public landing page and desktop web application",
  "type": "module",
  "workspaces": ["shared", "core", "landing", "web"],
  "scripts": {
    "dev:landing": "npm run dev --workspace landing",
    "dev:web": "npm run dev --workspace web",
    "build": "npm run build --workspace shared && npm run build --workspace landing && npm run build --workspace web",
    "test": "npm run test --workspace shared && npm run test --workspace core && npm run test --workspace landing && npm run test --workspace web",
    "typecheck": "npm run typecheck --workspace shared && npm run typecheck --workspace core && npm run typecheck --workspace landing && npm run typecheck --workspace web"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  },
  "engines": {
    "node": ">=20"
  }
}
